import { Module as Players } from "./players";
import { Module as Packages } from "./packages";
import { Module as Sessions } from "./sessions";

export const Modules = {
  Players,
  Packages,
  Sessions,
};
